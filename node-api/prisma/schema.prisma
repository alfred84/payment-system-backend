// Authoritative database schema — migrations in prisma/migrations/
// Full models added in Phase 3. Phase 0: datasource + client only.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
